name: adapt
channels:
  - conda-forge
dependencies:
  - python=3.11  # or 3.12, overridden by CI matrix
  - numpy
  - xarray
  - pandas
  - arm_pyart
  - opencv
  - scikit-image
  - matplotlib
  - cartopy
  - netCDF4
  - tqdm
  - h5py
  - proj
  - hdf5
  - libnetcdf
  - pip
  - pytest
  - pytest-cov
  - pip:
      - pydantic>=2.0
      - nexradaws
      - contextily
      - pyproj
      - -e .[dev]
