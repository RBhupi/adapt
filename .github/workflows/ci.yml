name: Adapt Continuous Integration

on:
  push:
    branches: [main, devel]
  pull_request:
    branches: [main, devel]

jobs:
  tests:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- Micromamba setup ---
      - name: Set up micromamba
        uses: mamba-org/setup-micromamba@v1
        with:
          environment-file: environment.yml
          cache-environment: true
          init-shell: bash
          condarc: |
            channels:
              - conda-forge
            channel_priority: strict



      # --- Install package in editable mode ---
      - name: Install Adapt (dev mode)
        shell: bash -l {0}
        run: |
          pip install --upgrade pip
          pip install -e .[dev]

      # --- Run tests ---

      - name: Show working directory before tests
        run: pwd

      - name: List files before tests
        run: ls -al

      - name: Run test suite
        shell: bash -l {0}
        run: |
          pytest -m "not integration" --cov=src/adapt

      - name: List files after tests
        run: ls -al

      # --- Upload coverage ---
      - name: List files for coverage debug
        run: ls -l

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.python-version }}
          path: .coverage
